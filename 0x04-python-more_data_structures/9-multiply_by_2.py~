#!/usr/bin/python3
def multiply_by_2(a_dictionary):
    if len(a_dictionary) == 0:
        return ()
    else:
        for k, v in a_dictionary.items():
            return v * 2
    return a_dictionary
