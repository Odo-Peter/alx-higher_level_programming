#!/usr/bin/python3
def max_integer(my_list=[]):
    if len(my_list) == 0:
        return ()
    else:
        temp = 0
        for i in my_list:
            if i > temp:
                temp = 1
        return temp
